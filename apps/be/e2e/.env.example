# The e2e suite starts its own server on this port and opens the same SQLite file.
# 3998, not the dev server's 3999, so a suite can run while `bun run dev` is up.
API_PORT=3998
APP_ENV=local
NODE_ENV=test
LOG_LEVEL=fatal
SQLITE_DB_PATH=./.tmp/e2e.db
E2E_API_URL=http://127.0.0.1:3998/api
